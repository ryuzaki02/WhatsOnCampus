<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:WhatsOnCampus.Model"
             xmlns:viewModel="clr-namespace:WhatsOnCampus.ViewModel"
             x:Class="WhatsOnCampus.Views.ProfilePage"
             Title="Profile Page">
    <ScrollView>
        <VerticalStackLayout>
            <Label Text="{Binding displayName}" FontSize="Title" HorizontalOptions="Center" VerticalOptions="Center" Margin="30"/>
        <CollectionView ItemsSource="{Binding Profiles}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Profile">
                    <VerticalStackLayout VerticalOptions="Center" Padding="10">
                    <Label Text="{Binding title}" Style="{StaticResource nameLabelStyle}"/>
                    <Label Text="{Binding subtitle}" Style="{StaticResource subNameLabelStyle}"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>            
    </ScrollView>
</ContentPage>
